@using Application
<header>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <div class="container-fluid">
            <div class="text-left">
                <ul class="navbar-nav">
                    <li class="nav-item">
                        <a class="navbar-brand" href="/">Word buddy</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="#">FAQ</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="#">Лента</a>
                    </li>
                </ul>
            </div>
            <div class="еуtext-right">
                
                <ul class="navbar-nav">
                    <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                        Меню
                    </a>
                    @if (User.Identity.IsAuthenticated) {
                        <li class="nav-item dropdown">
                            <ul class="dropdown-menu dropdown-menu-end dropdown-menu-dark" aria-labelledby="navbarDarkDropdownMenuLink">
                                <li><a class="dropdown-item" href="#">Профиль</a></li>
                                <li><a class="dropdown-item" asp-controller="setting" asp-action="Index">Настройки</a></li>
                                <li><a class="dropdown-item" asp-controller="user" asp-action="logOut">Выйти</a></li>
                            </ul>
                        </li>
                    }
                    <li class="nav-item">
                        <ul class="navbar-nav">
                            @if (!User.Identity.IsAuthenticated) {
                                <li><a class="nav-link" asp-controller="user" asp-action="login" asp-route-returnUrl="@ViewContext.HttpContext.Request.PathAndQuery()">Войти</a></li>
                                <li><a class="nav-link" asp-controller="user" asp-action="registration" asp-route-returnUrl="@ViewContext.HttpContext.Request.PathAndQuery()">Зарегистрироваться</a></li>
                            }
                        </ul>
                    </li>
                </ul>
            </div>
        </div>
    </nav>
</header>